(define (problem blocksworld-65)
    (:domain blocksworld)
    (:objects b1 - object)
    (:init
        (markable-0 b1)
        (clear b1)
        (on-table b1)
    )
    (:goal (and (clear b1) (on-table b1)))
)
